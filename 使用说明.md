# 分钟线买卖点信号看板 —— 使用说明

## 这是什么

一个配合文华财经使用的**分钟线信号辅助工具**。从公开行情接口抓取分钟 K 线，用技术指标 + 量价形态 + 多周期共振三层逻辑打分，输出「买点 / 卖点 / 观望」判断。

**不是自动交易程序，不会替你下单。** 是帮你交叉验证的看盘辅助。

---

## 页面结构（共 11 页）

顶部按钮条一排按钮，点哪个看哪个，每个都是**独立整页**：

| # | 页面 | 类型 |
|---|---|---|
| 1–7 | 棕榈油 · 豆粕 · 菜粕 · 豆油 · 菜油 · 鸡蛋 · 生猪 | 品种页 |
| 8–11 | 豆油-菜油 · 豆粕-菜粕 · 豆油-棕榈 · 菜油-棕榈 | 跨品种价差页 |

**每一页版式都一样**：顶部结论区（结论 + 当前值 + 长周期总结 + 月/周/日卡片 + 递进提示）→ 下方六张图（1/5/15 分钟 + 日/周/月线）。

品种页额外多一条风控参考条；价差页没有（价差的止损逻辑属于套利组合，这里不掺和）。

---

## 覆盖品种（7 个）

棕榈油 P0 · 豆粕 M0 · 菜粕 RM0 · 豆油 Y0 · 菜油 OI0 · **鸡蛋 JD0** · **生猪 LH0**

## 覆盖价差（4 个）

豆油-菜油 Y0-OI0 · 豆粕-菜粕 M0-RM0 · 豆油-棕榈 Y0-P0 · 菜油-棕榈 OI0-P0（均为主力连续对主力连续）

---

## 三种启动方式

### 一、云端版：打开网址就能看（手机 / 任何电脑）

如果你把这个项目放到了 GitHub，浏览器直接输入：

```
https://<你的用户名>.github.io/<仓库名>/
```

**不需要本机开机**，任何设备、任何网络都能打开。

- 更新方式：GitHub Actions 在交易日自动跑（早/午/夜盘时段，约每 4 分钟）
- 页面每 10 秒去拉一次数据文件，但**文件本身**由云端定时任务更新，
  所以实际数据新鲜度约 **3-10 分钟**（免费 runner 会排队，间隔不严格）
- 定位：看趋势、随时掏出手机瞄一眼，够用

> 想让它生效，需要先在 GitHub 仓库里把 Pages 的 Source 设为
> **GitHub Actions**（Settings → Pages → Build and deployment → Source）。

### 二、本机实时版：`启动看板.bat`（真正的 10 秒刷新）

双击后自动：
1. 在后台启动本地服务（标题为 `kanpan-server` 的窗口）
2. 浏览器打开 `http://127.0.0.1:8765/dashboard.html`
3. **数据每 10 秒自动更新**

**停止方式**：关闭标题为 `kanpan-server` 的窗口。

> 为什么必须走 HTTP 而不是直接打开文件？
> 浏览器对 `file://` 协议下相同路径的文件会强制走缓存，加时间戳参数也无效——你会一直看到旧数据。
> 本地 HTTP 服务带 `no-cache` 响应头，才能真正做到每次刷新读到最新数据。

### 三、备用：`刷新看板.bat`（单次刷新）

双击一次 = 抓一次数据 + 生成看板 + 打开浏览器。适合偶尔看一眼。生成后的 `dashboard.html` 是自包含文件，**离线也能打开**。

### 命令行

```bash
python build_dashboard.py              # 刷新全部品种
python build_dashboard.py P0 M0        # 只刷新指定品种
python serve.py --port 8765 --interval 10   # 启动实时服务
python serve.py --interval 5                # 改成 5 秒刷新
```

**云端与本机互不干扰**：云端那份跑在 GitHub 的服务器上，
你自己的电脑开不开机都不影响它；本机那份是独立的实时版，
两份数据各算各的，不会互相覆盖。

---

## 看板怎么读

### 顶部品种切换
7 个品种按钮横排，按钮上的小圆点颜色表示信号方向：**红=买、绿=卖、灰=观望**。数字是该品种的加权总分。

### 品种页顶部（核心信息区）

进入某个品种后，最上方依次是三块内容：

**① 品种名 + 结论 + 现价**
品种名右侧紧跟着当前结论（强烈买点/偏多/观望/偏空/强烈卖点）和最新价、涨跌幅（涨红跌绿）。

**② 递进提示条（本次新增的重点功能）**

按你要求实现的**信号递进升级**机制：

| 阶段 | 条件 | 提示级别 |
|---|---|---|
| 初步 | 1分钟 + 5分钟 均看涨 | L1 灰色「初步看涨」 |
| **强提示** | 叠加 15分钟 也看涨 | **L2 橙色「强看涨」** |
| **最强提示** | 叠加 30分钟 也看涨 | **L3 红色加粗「最强看涨」** |

看跌方向逻辑完全对称。

提示条右侧有**进度条**，显示四个周期哪些已确认（点亮=已确认），你能清楚看到信号卡在哪一环、还差哪个周期。

还包含几种"等待"和"否决"状态的明确提示：
- `仅1分钟线看涨，需5分钟线同步确认` —— 链条刚起步
- `1分钟与5分钟看涨，但15/30分钟方向相反（短多长空），逆势不做递进提示` —— 防止逆长周期方向操作
- `1分钟与5分钟多空信号互相矛盾，视为拉锯` —— 短周期自己打架

**③ 长周期面板（本次新增）**

日线 / 周线 / 月线三张卡片，每张包含：
- 收盘价与涨跌幅
- 倾向标签（偏多/偏空/中性）
- 一句话描述：**趋势 + 区间位置 + MACD状态 + RSI状态**

例如：`上行趋势，处于近60周期高位（88%）；MACD 红柱，动能减弱，RSI 70（超买区）`

上方还有**一句话总结**，把三个周期串起来：
`长周期共振偏多（月线偏多、周线偏多、日线中性）。注意：日线转弱但周线仍强`

若周期之间互相矛盾，会用橙色标签单独标出冲突点（如"日线转弱但周线仍强"），提醒你别被单一周期误导。

> **数据说明**：日/周/月线的"近60周期"分别指约 60 个交易日、60 周、60 个月。
> 周线和月线由日线聚合而成（按自然周/自然月分组，开=首日、高=最高、低=最低、收=末日、量累加）。
> 日线数据可追溯到 2007 年。

> **⚠️ 当日日K 是盘中拼出来的（重要）**
>
> 新浪的日线接口 `getDailyKLine` **只返回已收盘的日K，盘中不含当天那根**。
> 实测 2026-09-24 14:17（盘中）拉日线，最后一根还是 09-23 —— 也就是说，
> 如果不处理，**日/周/月线的图会少一根、长周期总结会停在昨天**，
> 当天的大幅波动（例如生猪当天跌 135 点）在长周期面板里完全看不到。
>
> 所以程序用**实时行情快照**拼出当日日K：开=今开、高=当日最高、低=当日最低、收=最新价、量=当日累计量，
> 追加（或覆盖）到日线末尾，**并且必须在聚合周/月线之前完成**，否则今天这根进不了本周/本月。
>
> 因此：
> - **日/周/月线是随刷新实时更新的**（跟分钟线一样 10 秒一次），不是"取昨天的"
> - 盘中那根是**未完成的**（收盘价还会跳），收盘后才定格
> - 每天的第一笔刷新会改变长周期判断 —— 这是正确的，因为当天的行情本来就该算进去


### 跨品种价差（独立页面）

顶部按钮条前半是 7 个品种，后半是 4 个价差，中间有「价差」小分隔。**每个价差都是一整页**，接在「生猪」后面。

**为什么价差值得单独看**：单合约价格会被整个板块的系统性涨跌带着走；价差是两条曲线的差，
系统性因素被抵消掉，剩下的基本是「两个标的谁强谁弱」的相对结构。
所以价差的趋势往往比单边价格更干净，也更适合做套利决策。

价差页与品种页**版式完全一致**（头部结论区 + 长周期总结 + 递进提示 + 六张图），只是：

- **标题**显示两条腿的名称与代码（如「豆油-棕榈价差 / Y0-P0」，下面小字标出「豆油（Y0）− 棕榈油（P0）」）
- **当前值**是该价差的最新数值（如 `+478` / `-1301`），红涨绿跌沿用同一套
- **六张图画的都是价差曲线**，不是任何一条腿的价格：分钟线是两腿按时间戳对齐后逐根相减，日/周/月线是价差自身的序列
- **长周期总结分析的是价差本身**的日/周/月线，不是两条腿各自的大级别走势
- **没有风控条**（价差是相对结构，止损/目标位在套利组合里有另一套算法，这里不掺和）
- **措辞不同**：跨品种价差用「走强 / 走弱 / 横盘」，而不是跨期月差那套「走扩 / 收敛」

当前启用的 4 个价差（两条腿均为主力连续）：

| 价差 | 代码 | 按钮顺序 |
|---|---|---|
| 豆油-菜油价差 | Y0-OI0 | 第 8 个 |
| 豆粕-菜粕价差 | M0-RM0 | 第 9 个 |
| 豆油-棕榈价差 | Y0-P0 | 第 10 个 |
| 菜油-棕榈价差 | OI0-P0 | 第 11 个 |

> **价差页零额外网络请求**：4 个价差的两条腿（Y0/P0/M0/RM0/OI0）全都在品种清单里，
> 分钟线与日线在抓取阶段已经有了，价差是用现成数据算出来的，不新增请求。
> 整个看板仍是 28 个请求、约 2 秒。

> **为什么不做跨期月差**：月差组合太多、穷举不完（每品种有 1-5、1-9、5-9……），
> 且随交割月推进要整体换月（如 `M2601/M2605` 交割退市后要改 `M2701/M2705`），
> 维护成本高且永远补不全。跨品种价差只有 C(4,2)=6 个组合，
> 两条腿都是主力连续、不存在换月，一次配好长期有效。

### 六图同屏

**1 分钟 / 5 分钟 / 15 分钟 + 日线 / 周线 / 月线，共六张图，3×2 排布在同一页面**（品种页与价差页都一样）：

| 周期 | 权重 | 定位 |
|---|---|---|
| 1 分钟 | 1 | 噪音较大，仅作参考 |
| 5 分钟 | 2 | 短线主战场 |
| 15 分钟 | 3 | **权重最高，最稳定** |
| 日线 | — | 中期方向（长周期面板解读） |
| 周线 | — | 大级别结构（长周期面板解读） |
| 月线 | — | 长期格局（长周期面板解读） |

> **30 分钟已从图上撤下**（你的要求）。数据仍然照抓 —— 它是最长周期的分钟线，
> 价差的结构判断与单合约的多周期共振评分都要用到它，只是不再单独占一张图。
> 所以「最强提示（L3）」依然要求 1+5+15+30 四周期全线共振。

每张图包含：K 线（红涨绿跌）、MA5（黄）、MA20（蓝）、成交量柱、RSI 线（紫），以及**红色向上三角=买点、绿色向下三角=卖点**。

日线及以上的量柱意义不大（价差的长周期序列量能常为 0），程序会自动省掉量柱把高度让给价格图。

每张图下方**「判断依据」**列出该周期触发了哪些具体信号。**这一栏最重要——不要只看结论，要看它基于什么得出的结论。**
长周期三张图因为不参与分钟级打分，这里改为展示该周期的长周期描述。

### 风控参考条
出现明确方向时给出：参考止损位、第一目标、盈亏比、加权总分。**盈亏比低于 1.5 建议放弃。**

---

## 信号逻辑（透明可查）

### 递进升级机制（核心）
看涨链路：`1分钟+5分钟` → 加`15分钟`=强提示 → 加`30分钟`=最强提示。
看跌链路完全对称。判定某周期"看涨"要求该周期原始分 ≥ 2。
**递进的前提是共振，不是打架** —— 短周期与长周期反向时不发递进提示。

### 三个关键约束（防止误报）
1. **至少 2 个周期同向** 才输出信号，单周期不算数
2. **长短周期显著对立时强制观望** —— 短周期（1/5分钟）合计分需达 ±4、长周期（15/30分钟）需达 ±3 且反向，才判定为对立。轻微反向不算（那只是震荡中的正常波动）
3. **分歧惩罚** —— 存在明显反向周期时按 45% 扣减总分

### 阈值（宁缺勿滥）
- 加权总分 ≥ **+9** 且 ≥2 周期看多 → 强烈买点
- 加权总分 +4~+8 → 偏多
- -3~+3 → 观望
- -4~-8 → 偏空
- ≤ **-9** 且 ≥2 周期看空 → 强烈卖点

### 指标与形态清单
- **均线**：MA5/10/20/60 金叉死叉、多头/空头排列
- **MACD**：金叉死叉、红绿柱放大、顶底背离
- **RSI**：超买(>75)/超卖(<25)、偏高偏低
- **布林带**：触及上轨/下轨
- **形态**：锤子线、射击之星、阳包阴、阴包阳、双底(W)、双顶(M)、突破/跌破前高前低
- **量价**：价涨量增、价涨量缩（背离预警）、缩量回调、价跌量增

### 长周期判定
日/周/月线各自综合三项打分：趋势（均线关系）+ MACD 柱状 + RSI 位置。
- 得分 ≥ +2 → 偏多，≤ -2 → 偏空，其余中性
- 三者加权（周线×3、日线×2、月线×2）合成总结论

### 跨品种价差判定（与单合约的三处关键差异）

> **历史沿革**：最初做的是跨期月差（如豆粕 1-5 月差 M2701-M2705）。
> 后来去掉了 —— 月差组合太多、穷举不完（每品种有 1-5、1-9、5-9……），
> 且随交割月推进要整体换月，维护成本高且永远补不全。
> 改为**跨品种价差**：4 个品种两两组合共 C(4,2)=6 个，是有限集合；
> 两条腿都用主力连续，不存在换月问题，一次配好长期有效。

价差的打分逻辑不能照搬单合约，因为踩过两个坑：

**坑一：各周期序列覆盖的时间差极大**
新浪接口对每个周期都返回固定约 1023 根，所以周期越长覆盖越久——
1 分钟线只有约 4 个交易日，30 分钟线却有约 4 个月。
如果直接在整段序列上算均线排列，等于拿 4 个月前的结构给现在下结论。
> 实测案例：棕榈油 1-5 月差过去 2.5 个月从 -44 走到 -466，
> 但最近一周一直贴在 -460 附近横盘。用全样本会报「强烈收敛」，
> 实际上走扩行情早就走完，当下是横盘。

**处理**：所有判断锚定在「与该周期时间等价」的近期窗口内：
| 周期 | 趋势窗口 | 结构窗口 |
|---|---|---|
| 1 分钟 | 120 根 | 240 根 |
| 5 分钟 | 48 根 | 160 根 |
| 15 分钟 | 32 根 | 120 根 |
| 30 分钟 | 24 根 | 96 根 |

**坑二：量纲差异大，固定阈值失效**
豆粕月差约 400、棕榈油约 -466、菜粕只有 -18；换成跨品种价差，
豆油-菜油在 -1300 附近、菜油-棕榈只有 +480。用固定数值判断突破，
会让小量纲的永远触发、大量纲的永不触发。
**处理**：所有判断按「波动单位」（类 ATR）归一化。

**方向判定用「路径效率」而不是「净变动/区间幅度」**
净变动/幅度这个指标很不稳定：豆粕 30 分钟在 60 根上算出 -6%（横盘），
在 120 根上却是 +41%（走扩），换个窗口结论就翻——因为区间幅度会被单根毛刺拉大。
改用 `路径效率 = 净变动 / 累计路径长度`：走直线接近 100%，反复震荡趋近 0。
实测区分度：纯噪音 1~5%，真趋势 13~21%。

> **注意**：路径效率的理论上限就是 100%（路径长度不可能小于净位移）。
> 波动单位取自 42 根窗口（而非标准 ATR 的 14 根）——取太短会把单位压得
> 接近 0，于是效率算出 500%、700% 这种物理上不可能的值。代码里另有一道
> `eff > 100` 的兜底：一旦越界就改用「净变动 / 区间幅度」这个有界比值。
> 冒烟测试 `smoketest.js` 已加入该上限的回归校验。

**措辞按类型切换**
同一套算法，两类价差用不同行话，避免读着别扭：

| | 价差变大 | 价差变小 | 强档 | 弱档 |
|---|---|---|---|---|
| 跨期月差 | 走扩 | 收敛 | 强烈走扩 | 偏走扩 |
| 跨品种价差 | 走强 | 走弱 | 显著走强 | 偏走强 |

代码里由 `kind` 参数（`calendar` / `inter`）驱动，见 `interprice.py` 的 `WORD` 映射表。

**结论自相矛盾的消解（结构优先于短周期评分）**
价差看的是相对结构，因此**结构一旦确认（效率 + 波动单位双重达标），权重高于分钟级评分**。

| 情形 | 处理 |
|---|---|
| 结构向上，结论也偏多 | 互相印证，保持原结论 |
| 结构向上，但结论偏空或震荡 | **由结构改判「偏走强」**，注明「短周期评分尚未跟上，属结构性走强的早期阶段」 |
| 结构向下，结论偏多或震荡 | 同理改判「偏走弱」 |
| 结论极端（显著），但结构横盘 | 降到「偏」一档；四周期共振则保留「偏」+「结构尚未确认」提示 |
| 结论震荡，结构也横盘 | 维持震荡（真正的无方向） |

> 实测案例：菜油总分 -5.2 落在震荡区间，但结构效率 17.6%、
> 净变动 9.2 个波动单位，是实打实的走强。若不改判，页面会同时出现
> 「结论：震荡」与「结构：走强」两句互相打架的话。改判后输出
> 「价差偏走强」+ 说明，逻辑自洽。

**配色强度（strength）与结论必须同号**
前端用 `strength`（-1~+1）决定文字颜色。若直接取 `总分/25`，
在被结构改判的场景下会出现「文字说走强、颜色却是绿的」。
因此当 `level != 0` 时以 level 为准做兜底，`level == 0` 时强制归零。
冒烟测试同样加了这个回归项。

**价差权重**：1分钟=1、5分钟=2、15分钟=3、30分钟=3（价差看结构，长周期权重更高）

**面板归属规则**
价差是两两之间的关系，但看板按品种分页，所以每个价差只挂到它的**前腿**页面上，
避免同一张图在两页重复出现。页面上同时标出两条腿的名字（如「豆油 − 菜油」）和代码（Y0-OI0）。

当前 4 个价差的分布：

| 价差 | 代码 | 所在页面 | 结论（2026-09-24 13:44） |
|---|---|---|---|
| 豆粕-菜粕价差 | M0-RM0 | 豆粕 | 偏走强 |
| 豆油-菜油价差 | Y0-OI0 | 豆油 | 偏走弱 |
| 豆油-棕榈价差 | Y0-P0 | 豆油 | 显著走强 |
| 菜油-棕榈价差 | OI0-P0 | 菜油 | 显著走强 |

> 另外两个组合（豆油-豆粕、菜油-菜粕）跨了油脂/粕类，相关性低、噪音大，
> 已在 `INTERPRICE_OPTIONAL` 里备好但默认不启用。要用只需把它挪进
> `INTERPRICE_LIST`，无需改任何其他代码。

**性能**：跨品种价差的两条腿都是主力连续，其分钟线本来就在抓取范围内，
所以价差**不产生任何额外网络请求**（相比月差方案的 68 请求降到 28 请求），
全流程从约 4.2 秒降到约 2.1 秒。

---

## 添加新品种

编辑 `fetch_data.py` 里的 `WATCH_LIST`：

```python
WATCH_LIST = {
    "P0": "棕榈油",
    "RB0": "螺纹钢",     # 加这一行
}
```

代码规则：**品种大写字母 + `0`**。常用：`RB0`螺纹 `I0`铁矿 `CU0`铜 `AU0`黄金 `TA0`PTA `MA0`甲醇 `SR0`白糖 `CF0`棉花 `C0`玉米。

---

## 文华麦语言公式

`wenhua_formula.txt` 可粘贴到文华客户端：

1. 文华财经 → 技术分析 → 指标管理器 → 新建指标
2. 整体粘贴，命名「买卖点信号」
3. K 线图右键 → 叠加指标 → 选中它

**注意**：麦语言表达不了多周期共振，该公式只实现了单周期逻辑。共振判断请看本地看板。

---

## 重要限制（务必阅读）

1. **数据源是新浪财经公开接口，不是交易所直连**
   存在秒级延迟。**仅供看盘辅助，绝对不能用于高频交易或抢单。**

2. **10 秒刷新会给接口持续压力**
   7 品种 × 4 周期（分钟线）+ 7 品种 × 3 周期（日/周/月，周月由日线聚合不算请求）= 28 个请求/轮，
   每 10 秒一轮 ≈ 每分钟 168 次。价差是用现成数据算的，不增加请求。
   已用 14 线程并发压到约 2 秒完成，但**长时间高频调用公开接口存在被限流的风险**。
   若发现数据异常，把间隔调回 30 秒（`python serve.py --interval 30`）。

3. **主力连续合约在换月时会有价格跳空**
   换月前后几天指标可能出现假信号，需自己留意。

4. **技术指标是概率工具，不是预测机器**
   任何信号都可能失效。本工具给的是「依据」，不是「答案」。

5. **本工具不构成投资建议**
   所有实盘决策及其后果由使用者自行承担。

---

## 文件说明

| 文件 | 用途 |
|---|---|
| `启动看板.bat` | **推荐**：启动实时刷新服务 |
| `刷新看板.bat` | 备用：单次刷新 |
| `serve.py` | 本地 HTTP 服务 + 定时刷新 |
| `fetch_data.py` | 抓取分钟线 + 日线（14 线程并发），聚合周/月线 |
| `indicators.py` | 技术指标计算 |
| `patterns.py` | 形态与量价识别 |
| `signal_engine.py` | 多周期共振打分 + **递进升级状态机** |
| `longterm.py` | **日周月线技术状态分析与总结**（品种与价差共用，纯函数） |
| `interprice.py` | **价差技术分析**（跨品种价差；同时兼容跨期月差）+ 价差日/周/月线聚合 |
| `build_dashboard.py` | 生成看板（统一 `pages` 结构；原子写入，防并发读坏文件） |
| `template.html` | 看板模板（品种页与价差页共用渲染代码） |
| `dashboard.html` | **看板文件** |
| `dashboard.data.json` | 纯数据包（供页面热更新，不整页刷新） |
| `wenhua_formula.txt` | 文华麦语言公式 |
| `smoketest.js` | 数据完整性自检（统一 pages 结构 + 价差一致性） |
| `domtest.js` | DOM 桩渲染自检（11 个页面各渲染头部 + 六图 + 长周期 + 递进） |
| `data/market_data.json` | 原始行情缓存 |

---

## 更新记录

**2026-09-24（第七轮：修复日/周/月线不含当天）**
**问题**：日线接口只返回已收盘日K，盘中不含当天那根 → 日/周/月线的图少一根，
长周期总结停在昨天，当天的大幅波动在长周期面板里看不到。
**修复**：新增 `fetch_data.merge_today_daily()`，用实时行情快照（开/高/低/最新价/成交量）拼出当日日K，
追加或覆盖到日线末尾；**调用位置在 `fetch_all` 里、聚合周/月线之前**（顺序错了今天这根进不了本周/本月）。
**验证**：新增冒烟回归「日线新鲜度检查」——逐页断言日线末根 == 今天、周/月线末根含本周本月。
修复前 11 页全部 STALE，修复后全部 OK。

**2026-09-24（第六轮：价差独立成页 + 六图版式）**
1. **4 个跨品种价差从「品种页下方的小面板」升级为独立整页**，接在「生猪」按钮之后（按钮条第 8~11 位）。
2. **每个页面统一为六张图**：1/5/15 分钟 + 日线/周线/月线，3×2 排布。**30 分钟从所有页面的图上撤下**（打分仍在用）。
3. **价差的长周期分析针对价差自身**：先把两条腿的日线对齐相减得到「价差日线」，再由它聚合出周线、月线
   （顺序不能反 —— 各自聚合再相减会因停牌/涨跌停缺日导致分组错位）。
4. **payload 结构统一**：`symbols` 数组 → `pages` 数组，每项带 `kind`（`symbol` / `interprice`）。
   前端品种页与价差页共用渲染代码，切页重绘逻辑也随之合一。
5. **递进提示支持参数化**：`escalation_signal(per_scores, weight, periods)`，
   价差沿用同一套 1+5 → +15 → +30 链路与跨品种措辞。
6. **图表细节**：`bars[tf]` 改为 `{bars, ind}` 打包结构；六图并排后单图变窄，
   日线及以上若量柱全为 0 则自动省掉量柱、把高度让给价格图。

**2026-09-24（第五轮：月差 → 跨品种价差）**
1. **移除跨期月差，改为跨品种价差**
   月差组合穷举不完且要随交割月换月；跨品种价差只有 C(4,2)=6 个，主力连续对主力连续，无换月问题。
   启用 4 个：豆粕-菜粕、豆油-菜油、豆油-棕榈、菜油-棕榈（另 2 个组合备而未用）。
2. **算法模块通用化**：`spread.py` → `interprice.py`，新增 `kind` 参数驱动措辞
   （跨期用「走扩/收敛」，跨品种用「走强/走弱」），数值逻辑完全共用。
   方向判断由中文比较改为 `dir_sign` 数值比较，消除措辞耦合。
3. **面板标题改为显示两条腿的名称**（如「豆油 − 棕榈油」），避免只看代码不知道在比什么。
4. **价差不再产生额外网络请求** —— 两条腿都是主力连续，分钟线本就在抓取范围内。
   请求数 68 → 28，全流程 4.2 秒 → 2.1 秒。

**2026-09-24（第四轮：修复 + 配色 + 月差）**
1. **修复「顶部区域时显时不显」**
   原因有两个：一是 `window.resize` 触发的 150ms 延迟重绘与 DOM 重建竞争；
   二是每 10 秒 `location.replace` 整页刷新会打断操作、造成闪烁。
   现已改为**局部热更新**：定时 `fetch('dashboard.data.json')` 拉数据后原地重绘，
   配合 `requestAnimationFrame` + 渲染令牌消除时序竞争，拉取失败才降级为整页刷新。
2. **顶部提示与长周期栏改为按多空着色**
   长周期总结按强度分 7 档（bull-3 ~ bear-3）配色；递进提示按级别 × 方向组合配色，
   L3 用深红/深绿底 + 左边框 + 外阴影，最强提示一眼可见。
3. **新增月差技术分析**（详见上文）
4. **月差算法的 4 处修正**（第四轮二次迭代，均已在 `smoketest.js` 加回归校验）

   | # | 问题 | 修正 |
   |---|---|---|
   | 1 | 波动单位窗口过短，效率算出 544%、769% 等物理上不可能的值 | `_atr_like` 窗口由 14 根改为 42 根；另加 `eff > 100` 兜底 |
   | 2 | `pos_periods`/`neg_periods` 用 `\|score\| >= 2` 门槛统计，与页面分周期颜色自相矛盾（豆粕四周期全多却记 3） | 改为按 `raw_score > 0 / < 0` 统计，门槛交给总分阈值把关 |
   | 3 | 结构已确认走扩/收敛，结论却停在「震荡」（菜油 case） | `level == 0` 不再提前返回；结构走扩/收敛时由结构改判「偏」，并说明「短周期尚未跟上」 |
   | 4 | 改判后 `strength` 仍按总分算，导致「文字多、颜色空」 | `level != 0` 时以 level 为兜底，`level == 0` 时强制归零 |

**2026-09-24（第三轮）**
- 新增递进升级提示（L1→L2→L3）
- 新增日/周/月线长周期分析
